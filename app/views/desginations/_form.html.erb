<%= form_for(@desgination) do |f| %>
  <% if @desgination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@desgination.errors.count, "error") %> prohibited this desgination from being saved:</h2>

      <ul>
      <% @desgination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
